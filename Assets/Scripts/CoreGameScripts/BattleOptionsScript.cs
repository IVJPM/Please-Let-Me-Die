using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class BattleOptionsScript : MonoBehaviour
{
    /*[SerializeField] GameObject buttonsHolder; 

    private void OnEnable()
    {
        PlayerManager.OnPlayerBattleState += PlayerManager_OnPlayerBattleState;
        PlayerManager.OnPlayerFreeState += PlayerManager_OnPlayerFreeState;
    }

    private void PlayerManager_OnPlayerFreeState(object sender, System.EventArgs e)
    {
        buttonsHolder.SetActive(false);
    }

    private void PlayerManager_OnPlayerBattleState(object sender, System.EventArgs e)
    {
        buttonsHolder.SetActive(true);
    }

    private void OnDisable()
    {
        PlayerManager.OnPlayerBattleState -= PlayerManager_OnPlayerBattleState;
        PlayerManager.OnPlayerFreeState -= PlayerManager_OnPlayerFreeState;
    }*/
}
